version: 2.1

workflows:
  main:
    jobs:
      - deploy:
          filters:
            tags:
              only: /production-.*/
            branches:
              ignore: /.*/
          environment: << pipeline.git.tag >>

jobs:
  deploy:
    parameters:
      environment:
        type: string
    steps:
      - run: some-command << parameters.environment >>
